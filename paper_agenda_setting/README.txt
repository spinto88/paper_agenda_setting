Para compilar:

escribir el comando "make" en la terminal, dentro del directorio donde se encuentre el Makefile

Para borrar archivos auxiliares:

escribir "make clean" y borra todos los archivos generados por latex 
